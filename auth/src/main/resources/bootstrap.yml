server:
  port: 10020
spring:
  application:
    name: auth
  cloud:
    nacos:
      discovery:
        server-addr: localhost:18080

# 打开监控端点
management:
  endpoints:
    web:
      exposure:
        include: "*"